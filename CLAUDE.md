# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Personal dotfiles backup + full-machine provisioning for **Ubuntu 26.04 ("resolute", arm64)**.
Everything here is Bash + config files — there is no build system, test suite, or linter.

## Commands
- `bash install.sh` — lightweight bootstrap: **shell + dotfiles only** (zsh, tmux, oh-my-zsh, p10k, core brew CLI, symlinks). Run as yourself, not under sudo.
- `bash provision/provision.sh --dry-run` — **the verification path**. Prints every planned action, makes no changes, needs no sudo. Run this after editing any step script.
- `sudo bash provision/provision.sh` — full machine replication. `sudo env PROVISION_USER=alice bash …` targets a user; `sudo env INSTALL_DESKTOP=1 bash …` adds the desktop/locale/IME set. **The canonical flag reference is `provision/README.md#flags`** (table + how they combine); `provision.sh --help` prints the recipes. Keep those two the owners — don't restate flag syntax elsewhere.
- `bash provision/inventory-export.sh` — run on the **source** machine to regenerate `packages/{apt.list,flatpak.list,Brewfile}` from live state.
- Scripts are **not** executable — always invoke with `bash <script>`.
- `bash smoke-test.sh lint && bash smoke-test.sh dry` — **the re-runnable smoke tier**: shellcheck the whole tree (warning severity; info notes are intentional) + a `--dry-run` matrix asserting exit codes and output markers for every flag scenario. Run after ANY script change. `bash smoke-test.sh verify S4` is the read-only end-state audit for a provisioned box/clone — **pass the runbook's scenario id** and it expands to the tokens (raw tokens and bare `verify` auto-detect still work; `scenario_tokens()` owns the map). Codifies the phase A–D hand audits; never calls Electron `--version`. Contradictory token combos (`full minimal`, `plain copy`, `installsh full`, `plain golden-clone`) are **refused with exit 2** before anything is audited. `bash smoke-test.sh scenarios` prints the live runbook (S1–S14) plus the id → token map and the composition rules.
- **Lint every script you touch with `shellcheck` — not just `bash -n`** (which only catches parse errors; shellcheck catches the bugs that actually bite: unquoted expansions, `$SUDO` redirect gotchas, etc.). shellcheck ships via the Brewfile. Run it from the steps dir so the sourced `lib.sh` resolves: `cd provision/steps && shellcheck -x <script>`. The tree is clean at the warning level; the only remaining notes are **intentional** info-level ones (SC2016 on the `as_user '…$HOME…'` deferred-expansion pattern, SC2024 on root-side `dconf load < "$FRAG"` redirects) — don't "fix" those.

## Layout
- `.zshrc`, `.p10k.zsh`, `.gitconfig` — the dotfiles (symlinked into `$HOME`).
- `install.sh` — lightweight bootstrap (see Commands).
- `provision/` — **canonical** full-machine replication. `provision.sh` is the entrypoint; numbered step scripts in `provision/steps/` (run 10→60), package lists in `provision/packages/`, shared helpers in `provision/lib.sh`. Details in `provision/README.md`.

## Architecture
`provision.sh` sources `lib.sh`, then runs `steps/*.sh` in numeric order, tolerating per-step failure and printing a summary. Order is load-bearing: 10-apt (base) → 20-apt-third-party (Docker/VSCodium/Cursor repos) → 25-docker-rootless (only `DOCKER_ROOTLESS=1`) → 30-brew → 35-rust (rustup) → 36-alacritty (`cargo install` + desktop integration + shell completions + vendored MesloLGS NF font) → 37-claude-code (native installer, stable channel) → 50-flatpak (adds Flathub remote) → 55-gnome-dconf (desktop-only) → 60-shell → 80-next-steps (`~/PROVISION-NEXT-STEPS.md` + self-silencing MOTD pointer — persists the manual follow-ups so a golden clone gets them; text = `lib.sh`'s `next_steps_text()`, shared with provision.sh's end-of-run print) → 90-finalize (only `GOLDEN_IMAGE=1`; leaves step 80's two files alone). There is **no snap step** — Alacritty (the only user snap) is built via cargo, so the machine needs no snapd.

`lib.sh` resolves a non-root `TARGET_USER` because cloud-init runs as root but **Homebrew, oh-my-zsh, rustup, and Claude Code refuse to / shouldn't run as root** — those steps drop to that user via `as_user` (a `sudo -u … bash -c` **non-login** shell — deliberately: a non-interactive login bash runs `~/.bash_logout`→`clear_console`, which fails without a tty and corrupted exit statuses; fixed in `0fa9b9c`). An explicitly-set `PROVISION_USER` **must exist or `lib.sh` dies** (catches a cloud-init typo before provisioning the wrong account; `--dry-run` only warns). Unset, it falls back `SUDO_USER` > uid 1000 > `ubuntu`.

### Editing step scripts — the helper contract
Every mutating action must go through a `lib.sh` helper so `--dry-run` stays accurate and runs stay non-interactive/idempotent. **Calling `apt-get`, `curl … | sudo tee`, etc. directly silently breaks `--dry-run`.**
- `run <cmd>` — runs a plain command, or prints `[would] …` under dry-run.
- `apt_get <args>` / `apt_install <pkgs>` — non-interactive apt (returns real status / tolerant). Wrapped in `env` so noninteractive+needrestart survive `sudo`, with `--force-conf*` and `DPkg::Lock::Timeout=600`.
- `as_user <cmd>` — run as `$TARGET_USER` via login shell (brew, oh-my-zsh, dotfile symlinks).
- For anything with pipes/redirects (key downloads, repo files), guard manually: `if dry; then would "…"; else …; fi`.
- `log`/`warn`/`die` for output; `dry`, `is_root`, `$SUDO`, `load_brew` are also provided.

### Ported dotfiles & configs
Both `install.sh` and step 60 install a shared set into `$HOME` — the file set is listed **once** in `dotfiles.list` at the repo root (single source of truth both read, so they can't drift): `.zshrc`, `.p10k.zsh`, `.gitconfig`, `.tmux.conf`, `.config/alacritty/alacritty.toml`, `.config/nvim/` (whole LazyVim tree; `lazy-lock.json` excluded so plugins track latest), and `.claude/{settings.json,statusline-command.sh}` (the repo mirrors `$HOME` layout; nested parents created; manifest entries may be files or whole directories). Default is **symlink** (repo stays source of truth); `DOTFILES_COPY=1` / `install.sh --copy` / `GOLDEN_IMAGE=1` **copy** instead (self-contained). The Alacritty theme repo is **cloned, not vendored** (step 36 / install.sh → `~/.config/alacritty/themes`). The **MesloLGS NF** Nerd Font, by contrast, **is** vendored (`fonts/MesloLGS-NF/` — no apt/brew package exists for it on Linux/arm64, and alacritty.toml pins it as the font family), copied into place system-wide by step 36 and into `~/.local/share/fonts` by install.sh (`.gitleaks.toml` allowlists the binary `.ttf` path). GNOME settings live in `provision/gnome/dconf-settings.ini`, applied by step 55 via `dconf load` **only when `INSTALL_DESKTOP=1`**. `.gitignore` keeps `.claude/` ignored *except* those two tracked files, and blocks the AI/API token files the config audit surfaced (`**/auth.json`, `**/.credentials.json`, `.claude.json`, `.codex/`, etc.). Configs with hardcoded `/home/<user>` paths are templated to `$HOME` so they survive a different target username.

### Modes (env flags)
- `--dry-run`/`-n` — preview only, no changes, no sudo, no network.
- `INSTALL_DESKTOP=1` — also install the desktop/locale/IME apt set + run the GNOME dconf step (55).
- `APT_UPGRADE=1` — before installing, `apt-get upgrade` the packages already on the box (step 10, right after `apt update`). **Off by default**: a blanket upgrade also bumps the installed kernel/grub/shim point-releases step 10 otherwise avoids (initramfs rebuild + a reboot-required flag provision.sh never acts on), so it is opt-in. Uses `upgrade` not `full-upgrade` (no new kernels), runs prompt-free via the noninteractive wrapper, and is convergent/idempotent. For a current golden image or a stale fresh box; cloud-init users can instead set `package_upgrade: true`. A reboot may be wanted afterward (a golden clone reboots anyway).
- `GOLDEN_IMAGE=1` — build a reusable image: implies **STRICT** (first real failure aborts), step 60 **copies** dotfiles instead of symlinking (self-contained), and the **finalize** step (90) resets machine-id / SSH host keys / cloud-init state / logs / history. Destructive — throwaway build box only. Clone the repo **and** write run logs under `/tmp` (finalize wipes `/tmp` last → auto-removed); finalize does **not** touch `$HOME`, but **warns** about a leftover repo / `*.log` there (hidden repos like `~/.oh-my-zsh` are skipped) so it isn't silently baked into the image.
- `DOTFILES_COPY=1` (or `install.sh --copy`) — copy dotfiles into `$HOME` instead of symlinking, *without* the rest of golden-image mode (`GOLDEN_IMAGE` implies it).
- `PROFILE=minimal` — lean headless agent box: steps 36-alacritty + 50-flatpak skip, step 20 skips the GUI editors (VSCodium/Cursor; **Docker stays**), steps 10/30 swap in `apt.minimal.list` / `Brewfile.minimal` (gh/gitleaks/jq/nvm/ripgrep/shellcheck; rust via step 35, node via nvm, claude via step 37). zsh+p10k+dotfiles kept — `.zshrc`'s bat/eza/zoxide niceties are `command -v`-guarded so the lean shell starts clean; `next_steps_text` drops the font bullet. Value validated (`full|minimal`, else die); **conflicts with `INSTALL_DESKTOP=1` (die)** — a minimal box has no GUI, and allowing both would half-install the desktop set.
- `DOCKER_ROOTLESS=1` — set up rootless Docker for `TARGET_USER` (step 25): runs the official `dockerd-rootless-setuptool.sh` as the user, enables linger + the `--user` service, and sets the `rootless` context. Needs the step-20 prereqs (`uidmap`, `docker-ce-rootless-extras`, `dbus-user-session`) and **unprivileged user namespaces** (host-dependent — soft-fails where userns is restricted; rootful Docker still works). On Ubuntu 24.04+/26.04 the restriction is AppArmor's `apparmor_restrict_unprivileged_userns` (on by default even when the `unprivileged_userns_clone` sysctl is `1`); relax it with a `sysctl.d` drop-in (`=0`) + `sudo sysctl --system` to enable rootless on clones. Do **not** also `usermod -aG docker` (that's the root-equivalent rootful path).
- Failure handling: tolerant helpers call `soft_fail` → warns + records to `$SOFT_FAIL_LOG`; a normal run completes every step but **exits non-zero** if any soft failure occurred. Under `STRICT`/`GOLDEN_IMAGE`, `soft_fail` and a failing step **die** immediately. `lib.sh` refuses a root/uid-0 `TARGET_USER`.

## Conventions / gotchas
- `provision.sh` is cloud-init-ready: root, idempotent, failure-tolerant. `--dry-run` previews with no changes and no sudo.
- Step 10 filters boot/kernel/third-party/desktop out of `apt.list` at install time (the list is the full `apt-mark showmanual` set on purpose); step 20 owns Docker/VSCodium/Cursor in **key → verify → repo → install** order — those packages have no repo at step 10. The repo add + install are **gated on a valid signing key** (`verify_keyring`): Docker, Cursor, and VSCodium fingerprints are all pinned (`DOCKER_FP`/`CURSOR_FP`/`VSCODIUM_KEY_FP`, each verified against the live key). A failed/mismatched key `soft_fail`s and skips that vendor rather than adding a half-configured repo. The post-repo index refresh is per-vendor and resilient (`vendor_apt_update`: only that vendor's source, retries, persistent failure warns even under STRICT — see TODO E, done). (Bruno was removed from the project — use httpie / VSCodium extensions instead.)
- Step 30 filters the `Brewfile` to `tap`/`brew`/`cask` lines; `flatpak`/`npm`/`mas`/`vscode` entries from `brew bundle dump` are dropped (flatpaks are owned by `flatpak.list`).
- `bat` must come from **brew** (binary `bat`); apt's is `batcat` and breaks `.zshrc`'s `alias cat="bat"`.
- **Casks are kept** in the Brewfile — Linux-capable casks (e.g. `codex`) install on linuxbrew arm64; macOS-only casks fail-soft.
- Arch-aware fallbacks: Docker codename falls back to `noble`. Cursor uses its signed apt repo (`downloads.cursor.com/aptrepo`, suite `stable`, both arches — codename-agnostic).
- Homebrew is hardcoded at `/home/linuxbrew/.linuxbrew`. The omz `jj`/`bun` plugins only add completions; a missing tool just warns.
- **Never committed** (see `.gitignore`): SSH/GPG keys, `~/.claude/`, cloud creds, shell history. `.gitconfig` ships a placeholder identity — set the real one.
- **Secret scanning** (defense-in-depth beyond the filename-based `.gitignore`): `gitleaks` (in the Brewfile) scans content. A versioned pre-commit hook lives in `.githooks/` — enable per clone with `git config core.hooksPath .githooks`. CI runs `.github/workflows/gitleaks.yml` on push/PR. `.gitleaks.toml` allowlists the pinned public key fingerprints (Docker/Cursor/VSCodium). The hook prefers `gitleaks git --staged`; CI scans full history.

## Open TODOs
- [x] **Live arm64 smoke-test — Phases A, B, C(Run 1) all PASSED** (fresh Ubuntu 26.04 arm64; A/B 2026-05-25, C Run 1 2026-05-27). **Phase A** (normal run): clean first run + idempotent re-run — apt base, Docker/VSCodium/Cursor signed repos, `brew bundle`, `rustup`, `cargo install alacritty` (0.17.0), Claude installer, vendored MesloLGS NF, completions, dotfile symlinks, `chsh`. **Phase B** (`GOLDEN_IMAGE=1`): STRICT + copy-mode + finalize, gate **PASS** (clean→exit 0) AND **ABORT** proven (`chattr +i` cred → `die`, exit 1); custom-named SSH key swept, `.docker/config.json` scrubbed keeping `currentContext`. **Phase C Run 1** (`GOLDEN_IMAGE=1 INSTALL_DESKTOP=1`): first live exercise of the **desktop apt set + dconf step 55 (headless via dbus-run-session) under STRICT golden** — passed; on-image smoke-test full green (toolchain incl. cursor 3.5.33 / codium 1.121 from the signed repos, dotfiles as copies, fonts, flatpak, dconf). 3 live-only bugs found+fixed across A/B: `as_user` login-shell exit-status corruption (`0fa9b9c`), `--dry-run` follow-ups leak (`0fa9b9c`), completions 0600→`compinit` deny (`92d247b`).
- [x] **Phase C Run 2 — PASSED** (2026-06-07, fresh arm64 26.04, mirrors the real UX on a stock userns-restricted box). **Run A** plain provision, zero prep (cloud-init path): exit 0 0; Cursor preseed+arch-pin clean on a fresh box; step 25 inert; **first live step-80 exercise** — notes file + MOTD drop-in correct, `run-parts` shows the pointer (NB: the banner needs a PAM login — SSH/console tty, NOT a GUI terminal). **Run B** negative (`DOCKER_ROOTLESS=1` while restricted): exactly ONE soft_fail at the new preflight (`2a0cb77`), exit 1 0, context stayed `default` (`d613f1b` fix held), rootful intact — idempotency proof #1. **Run C** followed the notes file literally (sysctl.d drop-in → re-run with the flag): rootless green end-to-end — context `rootless`, user service `active`, `Linger=yes`, hello-world via the rootless socket with its own image store (`~/.local/share/docker`) — idempotency proof #2. Live finding: `unshare --user --map-root-user true` is a FALSE-OK userns probe on 24.04+ (Ubuntu profiles the `unshare` binary; the restriction strips in-ns capabilities rather than denying creation) — check `/proc/sys/kernel/apparmor_restrict_unprivileged_userns` instead (step 25's preflight does). **Still pending from C:** the idempotency golden re-run (re-run on a built golden) was paused mid-flight — confirm it reaches `Finalize done` again (fold into D).
- [x] **A — persistent post-provision message — DONE** (2026-06-07). Step `80-next-steps.sh` writes `~/PROVISION-NEXT-STEPS.md` + an MOTD drop-in (`/etc/update-motd.d/99-provision-next-steps`) before finalize; both survive the golden wipe (finalize touches neither). Text = `lib.sh`'s `next_steps_text()` — single source of truth with provision.sh's end-of-run print. MOTD is self-silencing (gated on the `.md` existing — deleting the file when done is the entire cleanup). Verified: shellcheck-clean, `--dry-run`, temp-sandbox functional test, **live in Phase C Run 2** (tolerant run: file + MOTD pointer + content all correct on a fresh box; banner requires a PAM login — SSH/console, not a GUI terminal), and **golden-wipe survival proven live in Phase D** (both files present post-finalize on the Gen-2 image).
- [x] **B — `PROFILE=minimal` agent profile — SHIPPED** (2026-06-07; see Modes). Verified: shellcheck-clean, dry-run matrix (minimal markers in steps 10/20/30/36/50; full unchanged; bogus value + minimal+desktop both die), profile-aware `next_steps_text`, `zsh -n .zshrc`. **Live fresh-box run PASSED — scenario S11** (2026-08-05, bare Ubuntu 26.04 arm64, git-only bootstrap): dry-run then real run exit 0; every minimal skip fired live (VSCodium/Cursor/alacritty/flatpak; **Docker kept**), `apt.minimal.list` + `Brewfile.minimal` used; `smoke-test.sh verify minimal plain` all green (ABSENT assertions for codium/cursor/alacritty/flatpak/bat/eza/zoxide, shellcheck kept, font bullet absent from the notes file, `.zshrc` still a symlink); clean-shell test — p10k renders with **no `command not found` noise** (the `command -v` guards in `.zshrc` are what make the lean box quiet); idempotent re-run `✅ Provisioning complete — no failures.` Remaining untested sunny-days: S10 install.sh, S4 desktop-tolerant, S6 copy.
- [x] **D — iterate-on-golden test — PASSED** (2026-06-07; clone of the C Run 1 image → `GOLDEN_IMAGE=1 INSTALL_DESKTOP=1` re-run → Gen-2 golden captured). Proved: the **paused idempotency golden re-run** reaches `Finalize done` (apt/omz/claude skip; bring-to-latest real work where due — docker 29.5.2→29.5.3); **step 80's files survive the golden wipe** (notes + MOTD present post-finalize, fresh timestamps); the **Cursor migration** on a pre-arch-pin image (old both-arch `cursor.sources` → pinned + debconf preseed; NB step 10's full `apt update` runs BEFORE step 20 fixes an old source — runbook pre-checks `sudo apt update` and repairs first if drifting); E's `vendor_apt_update` live under STRICT; copies stayed copies (re-run carried `GOLDEN_IMAGE=1` — the trap to document for any manual re-run). Capture-readiness audit green (machine-id 0, no host keys, creds/tmp/history clear; `authorized_keys` correctly KEPT — cloud-init not re-injecting on a local clone). Post-finalize session residue (`~/.zsh_history` via `share_history`, terminal temp files) is expected — final sweep from an `unset HISTFILE` shell, then poweroff. Audit gotcha: never run Electron `--version` (codium/cursor) in a headless audit — it can hang with no output; use `dpkg-query -W codium cursor` instead.
- [ ] **XS — doc note:** "no non-root user" edge → require `PROVISION_USER` (don't auto-create users; cloud-init owns that).
- [x] **E — third-party apt-repo resilience — DONE** (2026-06-07). Step 20's per-vendor index refresh is now `vendor_apt_update`: refreshes **only that vendor's source file** (`Dir::Etc::sourcelist` + `Dir::Etc::sourceparts=/dev/null` + `APT::Get::List-Cleanup=0` — the cleanup flag is load-bearing: without it apt deletes every other repo's lists), 3 tries with 10/20s backoff, and a persistent failure **warns instead of soft_failing — even under STRICT**. Honest-underdelivery is preserved: base/security repos are untouched here *by construction* (step 10 owns them), and on a first run with no old index the `apt_install` right after still soft_fails → a golden still aborts on an actually-missing package, just no longer on a mere index hiccup (Cursor's recurring same-arch InRelease/Packages publish race). Bonus: no longer re-fetches the full Ubuntu index once per vendor. Verified: targeted update tested live against Cursor's repo (deb822 `.sources` accepted), shellcheck-clean, dry-run.
- [x] **S10 — `install.sh` live smoke — PASSED** (2026-08-05, fresh arm64 26.04; the second entry point's **first ever live test**). Symlink mode `verify installsh` 23/23; idempotent re-run made **zero** new backups (`unchanged()` guard holds); `--copy` flip clean (`.zshrc` not a symlink). **Two live-only bugs found+fixed:** (1) `b7b08c3` — no sudo priming, so `bash install.sh > log 2>&1` parked on an *invisible* password prompt (sudo prints to stderr, reads from /dev/tty) and looked hung; now `[0/7] sudo -v` up front, which also fails fast when the user has no sudo. (2) `81ff884` — install.sh shipped `.tmux.conf` and loaded omz's `tmux` plugin but never installed **tmux**, so every shell start nagged; tmux added to its apt line. The old "jj/bun may warn" note was **wrong** (both silent on a fresh box — tmux was the only nag); reworded. Harness gap fixed first (`2feb7e3`): `verify plain` was the wrong audit for an install.sh box (v_core asserts tmux/rustup/claude/docker/step-80 — none of which it installs), so **`verify installsh`** now audits its own contract and *replaces* v_core. Docs: `fa3d83c` — copy mode is **sticky** (a plain re-run won't restore symlinks; remove the files first), symlink→copy makes no backups, and the one `~/.zshrc.backup.<ts>` on a fresh box is oh-my-zsh's template. Clean-shell verified via `zsh -ic true` + tmux present.
- [x] **S4 — desktop (tolerant) live smoke — PASSED** (2026-08-05, fresh arm64 26.04 **desktop** image, live Wayland session). First run 36/36 + exit 0 no warnings; idempotency re-run 37/37 + exit 0 (`alacritty v0.17.0 is already installed` — no cargo rebuild; flatpaks/apt skip). **Two live-only bugs found+fixed (`f945b93`):** (1) **`apt_preflight` could hang longer than the problem it fixed** — `94eae3a` added it because apt "waits up to Lock::Timeout=600s and looks frozen", but its `systemd-run --wait --property=After=apt-daily…` barrier was UNBOUNDED. On a freshly booted desktop image `systemctl list-jobs` showed `apt-daily-upgrade.service start running` (unattended-upgrade, 27% CPU) with our transient unit `start waiting` behind it. Stopping the units first doesn't help: a stop queued behind a running *start* job waits for that job. Now 30s per `systemctl stop`, `APT_PREFLIGHT_TIMEOUT` (default 300s) on the barrier, the bound **announced before** waiting, and a timeout warns + continues (`DPkg::Lock::Timeout=600` was always the real backstop → the whole step is best-effort). (2) **The Nerd Font was installed but unused** — step 36 puts MesloLGS NF system-wide, yet GNOME's `monospace-font-name` stayed `Monospace`, so the *stock* terminal rendered p10k glyphs as tofu while Alacritty looked fine (it pins the family in `alacritty.toml` — which is why every previous desktop test missed this). Fixed with one machine-agnostic dconf key. Harness strengthened first (`a5b1a03`): `v_desktop` only checked "the fragment exists **in the repo**" — it now reads settings back off the box (`color-scheme`, `accent-color`, `monospace-font-name`), so a silently no-op `dconf load` can't pass.
- [x] **S6 — copy mode (tolerant) live smoke — PASSED** (2026-08-05, fresh arm64 26.04). `DOTFILES_COPY=1` without the rest of golden mode: exit 0, `verify copy full` 37/37 — dotfiles are real files not symlinks, and (via the harness work below) byte-identical to the repo. Note the gate I first proposed was **inert**: grepping the run log for `copy`/`self-contained` proves nothing, because step 60 emits those strings **only in its `--dry-run` branch** (`60-shell.sh:51-52`) — a real run logs nothing per-file. The `verify` token was the only real proof, which is why the content check was added to `v_mode` before running this.
- [x] **S12 — `APT_UPGRADE=1` bring-to-latest live smoke — PASSED** (2026-08-05, on the S6 box). The opt-in branch fired (`apt upgrade (APT_UPGRADE=1)` marker, `10-apt.sh:48`), `✅ Provisioning complete — no failures.`, `verify plain full` 37/37. Two findings, both **expected behaviour worth writing down** because they surprised the author: (1) **`139 → 43` upgradable is correct, not a partial upgrade.** Step 10 uses `apt-get upgrade`, never `full-upgrade`, so anything whose new version would **install a new package or remove an existing one** is held back — kernel ABI bumps (`linux-image-6.x.y-NN-generic` is a brand-new *name* each time), library transitions, `-dev` packages that gained a dep. That is precisely the "no new kernels" promise the flag exists to keep, so a permanent non-zero remainder is the design. Ubuntu's per-machine **phased updates** hold back a second, unrelated slice (`APT::Get::Always-Include-Phased-Updates` is deliberately not set in `apt_get`); `full-upgrade` wouldn't release those either. Split the two with `apt-get -s upgrade` vs `apt-get -s -o APT::Get::Always-Include-Phased-Updates=true upgrade`. NB `apt upgrade` (apt(8)) implies `--with-new-pkgs` while `apt-get upgrade` does not, so the "N can be upgraded" banner counts against a looser definition than what step 10 installs. (2) **`/var/run/reboot-required` appeared** — confirming TODO F item 7 live, and it is more than a docs problem: `provision.sh` *has* that state and discards it. Fix deferred to F (see item 7).
- [x] **S13 — cloud-init end-to-end — PASSED** (2026-08-16, AWS t4g.large arm64 Ubuntu 26.04, real `DataSourceEc2Local`; first run of provision.sh under production cloud-init rather than a hand-typed `sudo`). `cloud-init status` = done / `errors: []`, `✅ Provisioning complete — no failures.`, `verify plain full` **35/35, 0 skipped** run as the target user. Four things ran for the first time: (1) **`apt_preflight` under real cloud-init** — paused the updaters, announced its 300s bound, `no in-flight run`; no timeout, so `f945b93` holds in production. (2) **`chsh` with no controlling terminal** (`60-shell.sh:93`) — flipped `agent` to `/usr/bin/zsh` and left the login user at `/bin/bash`, i.e. exactly one account. (3) **`TARGET_GROUP` with primary group ≠ username** — the account's primary group was the *system* group `staff` (gid 50); every chown in the tree handled it. That code was added blind in the review-hardening round and had never executed. (4) **the uid-1000 fallback rung** — see below.
  - **Live gotcha that nearly invalidated the run: cloud-init does NOT assign uid 1000 in `users:` list order.** `- default` was listed first, yet the *second* entry got uid 1000 and the image default user got 1001. Since the whole design is "target a user that is NOT uid 1000, so a broken fallback is detectable", that collision made the explicit branch and the uid-1000 branch pick the same account — the headline assertion was unprovable from the live run alone. **Fix for next time: pin `uid:` explicitly in the user-data.**
  - **Recovered without a relaunch**, because branch selection is pure logic that `--dry-run` exercises exactly (no root, no changes). All three rungs of `lib.sh:73-87` proven to give *different* answers on the one box: `PROVISION_USER=probe` → `probe`; interactive with no override → the invoking user (`SUDO_USER`/`logname` rung); `sudo systemd-run --pipe … --dry-run` → the uid-1000 user. The third is the rung the golden-clone story depends on and it had never fired in any scenario (S2–S12 all set `PROVISION_USER=$USER`; S13 itself set it explicitly).
  - **`setsid` is NOT a cloud-init simulation** (my first probe was wrong): `logname` resolves from the audit loginuid in `/proc/self/loginuid`, which is inherited across `setsid`, so from inside an SSH session it still names the logged-in user. Only a systemd unit or real cloud-init has no loginuid. Use `systemd-run`.
- [x] **S14 — `PROFILE=minimal` + `DOCKER_ROOTLESS=1` live smoke — PASSED** (2026-08-21, fresh arm64 26.04, userns relaxed first). **The live smoke series is now COMPLETE — every sunny-day scenario S1–S14 has run on real hardware.** `✅ Provisioning complete — no failures.`, `verify minimal plain rootless` **37/37**. The combination was the point: the lean profile drops the GUI set (`alacritty`/`flatpak`/VSCodium/Cursor skipped, `Brewfile.minimal`) while **keeping Docker and its rootless prerequisites** — proven live, not just by the code path. Decisive evidence was `docker info`, not `docker context show`: `SecurityOptions` contained `name=rootless` and `DockerRootDir` was `~/.local/share/docker`. A context named `rootless` pointing at the rootful daemon would satisfy the context check and fail this one — that is the `d613f1b` failure mode, and it is why the audit must read the daemon rather than the label. Two things ran for the first time on a box with **no desktop stack**: `docker-ce-rootless-extras`/`uidmap`/`dbus-user-session` installing under `PROFILE=minimal` (step 20's Docker block is deliberately *not* profile-gated), and step 25's `/run/user/$uid/bus` wait succeeding with `dbus-user-session` alone carrying it (daemon came up on `/run/user/1000/docker.sock`, `Linger=yes`). Harness strengthened first (`a28eb4d`): the dry matrix had a `rootless` case and a `minimal` case but never the combination, so a `minimal &&` gate added to step 20's Docker block would have passed both while breaking rootless on the one profile most likely to want it (30 → 35 dry assertions). Live finding, filed as TODO F item 10: the notes text still recites the whole "how to set up rootless" recipe **after a run that just set it up**.
- [ ] **F — UX + docs simplification pass** (raised 2026-08-05; unblocked 2026-08-21; **items 1-5 shipped 2026-08-21, 6-10 open**). Everything works, but the *interface* has outgrown its documentation. Concrete problems, not a vague tidy-up:
  1. ✅ **DONE (2026-08-21) — recipe layer.** Root README opens with **"Pick your box"**: 5 named one-line recipes (shell-only / headless agent / daily desktop / golden image / bring-to-latest), then `--dry-run` as the universal preview, then the three things that are *add-ons* rather than recipes (`DOCKER_ROOTLESS`, `PROVISION_USER`, cloud-init). The old "Two ways to use it" stub is gone — its framing is now the table's first paragraph — and "Quick start (install.sh)" became "`install.sh` in detail", i.e. the recipe's detail view rather than a competing entry point.
  2. ✅ **DONE (2026-08-21) — one canonical flag table.** `provision/README.md#flags`: every flag with default + effect, followed by a **"How they combine"** list that finally writes down the implications in one place (`GOLDEN_IMAGE ⇒ STRICT + DOTFILES_COPY` and why `STRICT=0` can't weaken it; `minimal ⊥ INSTALL_DESKTOP`; `GOLDEN_IMAGE + DOCKER_ROOTLESS` unsupported and why; copy-mode stickiness; UPPERCASE-only). Root README and `--help` link to it; this file's Commands line now names it as the owner. Two flags were **documented but unguarded** — `STRICT` and `DOTFILES_COPY` were missing from provision.sh's lowercase-typo warning loop; added, so the table doesn't promise a guard that isn't there. Also standardized every example on `sudo env VAR=… ` (per sudo(8) a bare `sudo VAR=…` is subject to the sudoers policy and is *refused* under a restricted rule without the `SETENV` tag; `env` sidesteps it, and it's the form every live scenario used).
  3. ✅ **DONE (2026-08-21) — `--help` answers "which one do I want".** It now prints the recipes, the `--dry-run` preview line, the combinable add-ons, and a pointer to the flag table. Asserted in `smoke-test.sh dry` (exit 0 + the recipe markers + the `#flags` pointer) so a rewrite can't quietly drop them. Found while adding it: `dry_case`'s `grep -qF "$p"` parses a pattern starting with `-` as an option — `--dry-run` was reported MISSING when it was present; fixed with `grep -qF -e`.
  4. ✅ **DONE (2026-08-21) — one vocabulary.** `verify S4` works and echoes what it expanded to; `scenario_tokens()` is the single owner of the map. The runbook no longer speaks tokens at all — every entry now says `-> verify S<n>`, so there is nothing left to hand-translate. `scenarios` prints an id → token table **generated from the same function**, so the two can't drift again, and `dry` asserts that every S-id the runbook prints resolves (S1 correctly resolves to "nothing to audit" — it *is* the lint+dry tier), that lowercase works, and that an unknown id is rejected. Add a scenario, forget the map, and it goes red on the dev box instead of on a live one.
  5. ✅ **DONE (2026-08-21) — composition rules stated AND enforced.** `scenarios` now ends with the vocabulary grouped by kind rather than as a flat list: MODE (exactly one — `plain`/`copy`/`golden-clone`, the last implying `copy`), PROFILE (exactly one — `full` asserts the GUI set present, `minimal` asserts it absent), ADD-ONs (`desktop`/`rootless`, order-free), and `installsh` as STANDALONE because it *replaces* the core audit. Then, because a documented-but-unenforced rule is the same trap as item 2's missing typo guard: `verify_validate()` **refuses** the contradictory combos with exit 2 before auditing anything, and `golden-clone copy` drops the redundant token instead of double-counting. Without that, `verify full minimal` printed a wall of red for the wrong reason — each token correctly asserting the other's negation — which reads as a regression rather than operator error. The dry-tier test pins **exit 2** specifically, not merely non-zero: a broken guard would let the real audit run, and a failing audit is also non-zero, so "non-zero" would have been a false green.
  8. **Font advice went stale the moment the dconf key landed.** `next_steps_text`'s full-profile bullet still says *"in any OTHER terminal, select MesloLGS NF in its font settings"* — but since `f945b93` set GNOME's `monospace-font-name`, the stock terminals (Console / gnome-terminal / gedit) are handled automatically and that sentence is now only true for non-GNOME terminals. One-line reword, folded here rather than fixed in isolation because the same sentence is echoed by the on-box notes file — a #6 duplication instance, so fix owner-first.
  7. **Surprising-but-intended behaviour has no voice at the point of use.** Three instances, same shape — the run *knows* something the user needs and stays quiet:
     - `APT_UPGRADE` being off by default means a freshly provisioned box still reports "N packages can be upgraded" — correct (see Modes) and documented in CLAUDE.md + the step comment, but nothing says it at the end of a run, where it would land. Surprised the repo's own author (2026-08-05).
     - **With `APT_UPGRADE=1` the count still isn't zero** (S12 live: 139 → 43) because `apt-get upgrade` holds back new-package/removal cases and phased updates. Correct by design, unexplained anywhere the user is looking.
     - **`/var/run/reboot-required` is discarded** (S12 live: the flag was set and nothing said so). Unlike the other two this is *dynamic machine state*, not documentation, so it cannot live in `next_steps_text` — that text is static and shared byte-for-byte with the on-box notes file. It needs an end-of-run print in `provision.sh` guarded by `! dry && [ -e /var/run/reboot-required ]` (plus `reboot-required.pkgs` if readable). **Caveat to decide when implementing:** `GOLDEN_IMAGE=1` exits before that point, so a golden build wouldn't print it — defensible (a clone reboots anyway) but it silences the message on the very path where `APT_UPGRADE=1` is most likely used.
     Candidate for the first two: one `next_steps_text` bullet — but weigh against #6 before adding more text. The third is code, not text.
  6. **Six surfaces restate the same facts** (root README, provision/README, CLAUDE.md, `next_steps_text`, the on-box notes file, MOTD, `smoke-test.sh scenarios`) → drift. **Already bit us**: the "jj/bun may warn" claim was false in *both* install.sh's closing note and the README (fixed `81ff884`/`fa3d83c`). Fix: one owner per fact.
  9. **The MOTD banner points every OTHER user at a file they cannot read** (found live in S13, 2026-08-16 — the first run where target user ≠ login user). Step 80 bakes an absolute path in at write time (`80-next-steps.sh:50`), so logging in as the image's default user prints `>>> … see /home/agent/PROVISION-NEXT-STEPS.md` — but `$TARGET_HOME` is mode 0750, so that user gets EACCES. `pam_motd` runs the drop-in as root with no reliable per-login identity, so "only show it to the target user" is not straightforwardly available. Options, in increasing cost: (a) reword the pointer to name the owner and how to read it (`sudo -iu agent cat …`) — one line, no new file; (b) also write a root-owned 0644 copy to a shared path (`/usr/local/share/…`) and point the banner there, keeping the home copy for the target user — costs a #6 duplication instance, though both would come from `next_steps_text`; (c) move the canonical file to the shared path outright and drop the home copy (loses the golden-wipe-survival property that step 80 was built for). **Low severity — invisible whenever target == login user, which is every other scenario — but cloud-init is the primary target, so it is not exotic.** Belongs here rather than as a standalone fix because F is already rewriting this exact surface.
  10. **`next_steps_text` gives advice the run has already carried out** (found live in S14, 2026-08-21). After `DOCKER_ROOTLESS=1` succeeds, the end-of-run text *and* the on-box notes file still print the full "Docker access — choose ONE" recipe: re-run with `DOCKER_ROOTLESS=1`, run the setuptool, `docker context use rootless`, and how to relax userns if it fails (`lib.sh:241-252`). All of it is already done. Same shape as items 7 and 8 — text that is stale at the point of use — but the fix is cheaper than either, because `next_steps_text` **already branches on state**: it takes a `minimal` branch for the font bullet, so a `DOCKER_ROOTLESS`/context-aware branch is the same move. Note the branch must key off what actually *happened* (step 25 can soft-fail on a restricted host and leave the box rootful), not merely off the flag being set.
- [ ] **Later — packer + chezmoi split** (long-stated future layer).

## Project direction & philosophy
- **Two entry points, both first-class:** `install.sh` = lightweight shell+dotfiles on an *existing* box; `provision/` = full machine / cloud-init / golden image. They share the dotfile set via `dotfiles.list`.
- **Bring-to-latest is intentional, not a gap:** brew / `cargo install` / `rustup` / Claude+Cursor stable channels all float. "Reproducible" here means *re-runnable to the same state* (idempotent), **not** version-pinned. Don't add default version pins — bit-for-bit pinning, if ever wanted, belongs at a future **packer** layer.
- **The repo's dual role is permanent** (even after a possible future packer + chezmoi split): (1) fresh Ubuntu → clean, cred-free state — no golden image is built from the owner's daily box because it holds creds; (2) a re-runnable "bring-to-latest" layer on top of an existing golden image to keep it current.
- **No creds/PII ever** — enforced by `.gitignore` + the package-names-only `inventory-export.sh`.
